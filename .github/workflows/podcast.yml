name: Build Podcast Feed

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install Node dependencies
        run: |
          npm init -y
          npm install @xmldom/xmldom node-fetch

      - name: Fetch source feeds
        run: |
          curl -L "https://feeds.feedburner.com/meyersonstrategy/podcasts" -o feed1.xml
          curl -L "https://feeds.feedburner.com/chicagopublicsquare/podcasts" -o feed2.xml

      - name: Merge feeds with Node.js
        run: |
          node scripts/merge-feeds.js

      - name: Commit and push changes
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add podcast.xml
          git commit -m "Update podcast feed" || echo "No changes to commit"
          git push


      - name: Commit and push changes
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add podcast.xml
          git commit -m "Auto-update merged podcast feed" || true
          git push

      - name: Ping WebSub hub
        run: |
          curl -X POST https://pubsubhubbub.appspot.com/publish \
            -d "hub.mode=publish" \
            -d "hub.url=https://charlesmeyerson.github.io/podcast.xml"
